#!/bin/bash
# 解压前回调：必须快退

mkdir -p "${TRIM_PKGVAR}/log" 2>/dev/null || true
echo "[$(date '+%Y-%m-%d %H:%M:%S')] install_init ok (native)" >> "${TRIM_PKGVAR}/log/install.log" 2>/dev/null || true
exit 0
